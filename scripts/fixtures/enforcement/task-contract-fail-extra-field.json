{
  "contractVersion": "1.0.0",
  "taskId": "t_fixture_extra_field",
  "purpose": "Verify that the zero-dependency validator matches the canonical schema's closed object shape.",
  "inputs": [
    {"path": "AGENTS.md", "required": true, "verify": "exists", "unexpected": true}
  ],
  "outputs": [
    {"path": "/tmp/akm-task-contract-extra-field-report.json", "kind": "report", "required": true}
  ],
  "canonicalPath": "/tmp/akm-task-contract-extra-field-report.json",
  "format": ["UTF-8 JSON"],
  "sources": [
    {"ref": "99-system/TASK-CONTRACT-SCHEMA.md", "role": "source-of-truth"}
  ],
  "owner": {"primary": "operator", "handoff": "after focused verification passes"},
  "failureHandling": {
    "blockedWhen": ["an external runtime dependency is unavailable"],
    "holdWhen": ["an internal verification gate is unmet"],
    "preserve": ["report output and command exit code"]
  },
  "completion": {
    "required": ["required report exists and reads back"],
    "optional": []
  },
  "verification": [
    {
      "command": "test -f /tmp/akm-task-contract-extra-field-report.json",
      "expectedExit": 0,
      "readback": "/tmp/akm-task-contract-extra-field-report.json"
    }
  ],
  "runtime": {"maxSeconds": 2700, "goalMode": false},
  "nonGoals": ["modify input documents"],
  "securityExclusions": ["auth", "token", "cookie", "session-db", "secret"],
  "blockers": []
}
